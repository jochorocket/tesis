\documentclass{article}
\usepackage{pgfplots}
\usepackage[spanish,mexico]{babel}
\pgfplotsset{compat=1.10}

\begin{document}
\pagestyle{empty}

% Preamble:
\begin{tikzpicture}
    \begin{axis}[view={60}{30},	
	    scale only axis,
	    title=Funci\'on Benchmark: Arckley,
	    xlabel={$x$},
	    ylabel={$y$},
	    grid=major,
	    legend cell align=left,
	    /pgfplots/enlargelimits=false,
	      legend style={legend pos=north,font=\tiny},
	    %small,
    ]
    \addplot3[surf,mesh/ordering=y varies,domain=-5:5] {-20*exp(-0.2*sqrt(0.5*(x^2+y^2)))-exp(0.5*(cos(2*pi*x)+cos(2*pi*y)))+e+20};
    %\addlegendentry{$-20*exp(-0.2*sqrt(0.5*(x^2+y^2)))-exp(0.5*(cos(2*pi*x)+cos(2*pi*y)))+e+20$};
    \end{axis}
   \end{tikzpicture}
\end{document}
